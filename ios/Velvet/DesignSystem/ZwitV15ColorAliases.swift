import SwiftUI

/// Compatibilité v1.5 : les nouvelles surfaces utilisent des noms Zwit courts,
/// mais restent branchées sur les tokens historiques uniques du design system.
extension Color {
    static let velvetGold = VelvetColor.champagneGold
    static let velvetBurgundy = VelvetColor.velvetBurgundy
    static let velvetBlack = VelvetColor.velvetBlack
}
