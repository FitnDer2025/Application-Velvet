name: Velvet industrialization CI

on:
  push:
    paths:
      - "apps/api/**"
      - "apps/beta/**"
      - "packages/api-client/**"
      - "docs/beta/**"
      - "functions/**"
      - "infra/**"
      - "scripts/check-supabase-migrations.mjs"
      - "package.json"
  pull_request:
    paths:
      - "apps/api/**"
      - "apps/beta/**"
      - "packages/api-client/**"
      - "docs/beta/**"
      - "functions/**"
      - "infra/**"
      - "scripts/check-supabase-migrations.mjs"
      - "package.json"

permissions:
  contents: read

jobs:
  security-and-contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24.18.0"
      - run: npm install --ignore-scripts
      - run: npm run check
      - run: npm test
      - run: npm run build:beta
