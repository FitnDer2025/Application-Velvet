name: Apply Zwit iOS experience

on:
  push:
    branches:
      - feat/zwit-brand-messaging-albums-splash
    paths:
      - scripts/apply-zwit-ios.py
      - .github/workflows/apply-zwit-ios.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply targeted Swift patches
        run: python scripts/apply-zwit-ios.py
      - name: Commit generated changes
        run: |
          git config user.name "Velvet Application"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "feat(ios): dates, albums et ouverture Zwit [feature-applied]"
          git push origin HEAD:feat/zwit-brand-messaging-albums-splash
